# Base URL of the running Branch Teller REST API (see ../src/main/java/com/branchteller/api).
# Copy this file to .env and adjust if the API isn't on the default port/host.
# Default matches ApiServer's API_PORT default (8082) and docker-compose's api service.
VITE_API_BASE_URL=http://localhost:8082
